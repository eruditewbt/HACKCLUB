version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: dataanalysis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - da_pgdata:/var/lib/postgresql/data
volumes:
  da_pgdata:
