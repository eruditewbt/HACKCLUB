# Schema Prompt Lab (No Dependencies)

Goal: start from `llm_prompt_with_schema.py` and build a safe “parse + validate + explain errors” layer.

## Files
- `schema.py`: minimal schema checks for JSON outputs
- `demo.py`: example parse/validate flow

## Run
```bash
python -m course_snippets.labs.schema_prompt_lab.demo
```
