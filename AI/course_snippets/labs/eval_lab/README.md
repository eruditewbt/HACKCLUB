# Eval Lab (No Dependencies)

Goal: extend `model_eval_template.py` into a tiny evaluation harness that supports:
- datasets
- multiple metrics
- basic error analysis

## Run
```bash
python -m course_snippets.labs.eval_lab.demo
```
