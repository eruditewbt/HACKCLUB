# Vision (Scaffold)

This directory is for image + document vision.

Planned modules:
- OCR
- layout + table extraction
- captioning / VQA
- detection

Implementation will likely use optional dependencies (e.g. Tesseract, OpenCV, transformers).
