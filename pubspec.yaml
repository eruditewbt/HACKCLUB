name: translate_tool_v3
description: A powerful CLI and web tool for managing Flutter translations with extraction, merging, and auto-translation.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  yaml: ^3.1.2
  http: ^1.1.0
  path: ^1.9.0
  args: ^2.4.0

dev_dependencies:
  test: ^1.24.0
